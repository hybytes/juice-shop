name: Main Build Pipeline
on: [push, pull_request]
jobs:
  security-check:
    # Calls your library repo
    uses: hybytes/shared-security-pipelines/.github/workflows/security-gate.yml@main
    secrets: inherit
